language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jingjielim/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jingjielim/docker-react npm run test